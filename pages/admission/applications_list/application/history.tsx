import ApplicationHistoryPage from "@/src/components/page_components/admission_pages/application_list_page/application_page/application_history_page";
import { GetServerSideProps } from "next";
import { serverSideTranslations } from "next-i18next/serverSideTranslations";
import Head from "next/head";

export default function AdmissionApplicationListMainHistory() {
  return (
    <>
      <Head>
        <title>История событий</title>
        <meta name="description" content="Generated by create next app" />
      </Head>
      <ApplicationHistoryPage />
    </>
  );
}

export const getServerSideProps: GetServerSideProps = async ({ locale }) => {
  return {
    props: {
      ...(await serverSideTranslations(locale as string, ["common"])),
    },
  };
};
